{% extends 'base.html.twig' %}

{% block title %}Edit Releve{% endblock %}

{% block body %}
    <h1>Edit Releve</h1>

    {{ include('releve/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_releve_index') }}" class="btn btn-primary">back to list</a>

    {{ include('releve/_delete_form.html.twig') }}
{% endblock %}
