{% extends 'base.html.twig' %}

{% block title %}Releve{% endblock %}

{% block body %}
    <h1>Releve</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ releve.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ releve.Date ? releve.Date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>ReleveBrut</th>
                <td>{{ releve.ReleveBrut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_releve_index') }}">back to list</a>

    <a href="{{ path('app_releve_edit', {'id': releve.id}) }}">edit</a>

    {{ include('releve/_delete_form.html.twig') }}
{% endblock %}
